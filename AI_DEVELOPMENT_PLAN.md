# AI 개발 계획

## 1. 문서 목적

이 문서는 `clair-ai` 저장소에서 담당하는 AI 파트 개발 범위와 구현 순서를 실행 계획 형태로 고정하기 위한 문서다. 팀 공통 문서인 `../team-docs`의 목표, 일정, 역할, 프로그램 로직을 바탕으로 작성하되, 프론트엔드와 백엔드 전체 계획을 복사하지 않고 이 저장소의 실제 책임 범위에 맞게 재구성한다.

본 문서의 기준일은 2026년 3월 17일이며, 목표 수준은 4주 안에 시연 가능한 계약서 분석 AI 파이프라인 확보다.

## 2. 프로젝트 목표와 MVP

### 목표

- PDF, 이미지, 텍스트 형식 계약서를 입력받아 분석 가능한 AI 파이프라인을 구축한다.
- OCR, 조항 분리, 핵심 정보 추출, 위험 조항 분석, 요약, 질의응답, 시각 요소 탐지를 하나의 흐름으로 연결한다.
- 결과는 백엔드/API 팀이 저장 및 노출할 수 있는 구조화 형태로 반환 가능해야 한다.

### MVP 범위

- 지원 계약 유형은 `NDA`, `용역계약서`, `근로계약서` 3종으로 제한한다.
- 분석 대상 기능은 아래 항목으로 한정한다.
  - OCR 기반 텍스트 추출
  - 조항 분리
  - 계약 유형 분류
  - 핵심 필드 추출
  - 위험 조항 탐지
  - 계약서 요약
  - 근거 조항 기반 질의응답
  - 인감, 서명, 체크 표식 탐지

### 제외 범위

- 프론트엔드 화면 구현
- FastAPI 라우터 상세 구현
- MySQL 스키마 구현 자체
- 판례 기반 고급 법률 자문
- 모든 계약 유형 일반화
- 자동 수정 제안 생성

## 3. 저장소 책임 범위

### 포함 디렉터리

- `src/chains`: LangChain 기반 분석 체인과 LLM 오케스트레이션
- `src/ocr`: PaddleOCR 기반 텍스트 추출 파이프라인
- `src/vision`: YOLOv8 기반 시각 요소 탐지 파이프라인
- `src/prompts`: 분류, 추출, 요약, 질의응답 프롬프트 자산
- `src/evaluation`: OCR, 추출, 탐지, 요약, 질의응답 평가 기준과 결과 관리
- `training`: YOLOv8 학습 설정, 실험 관리, 데이터셋 메타정보

### 제외 디렉터리 및 책임

- React/PWA 사용자 화면
- FastAPI API 명세와 라우팅 로직
- DB 테이블 생성 및 운영
- 배포 인프라와 서비스 운영

### 저장소 기준 우선순위

현재 코드 상태상 [contract_analysis_chain.py](/Users/choseohyun/dev/clair-ai/src/chains/contract_analysis_chain.py#L1) 는 체인 골격이 비어 있고, [README.md](/Users/choseohyun/dev/clair-ai/src/evaluation/README.md#L1) 는 평가 기준이 미정이다. 따라서 실제 구현 순서는 다음 기준으로 잡는다.

1. 분석 체인 입력/출력 골격 정의
2. 평가 항목과 결과 저장 규칙 정의
3. OCR, 파서, 추출 파이프라인 연결
4. 요약, 위험 분석, 질의응답 연결
5. 비전 탐지와 텍스트 분석 결과 통합

## 4. AI 파이프라인 구현 순서

### 전체 흐름

```text
문서 입력
-> 파일 유형 판별
-> OCR 수행 또는 텍스트 직접 사용
-> OCR 정제 및 전처리
-> 조항 분리
-> 필드 후보 추출
-> 계약 유형 분류
-> 핵심 정보 구조화 추출
-> 위험 조항 분석
-> 요약 생성
-> 질의응답
-> 시각 요소 탐지 결과 결합
-> 결과 패키징 및 평가
```

### 구현 원칙

- 구조화 출력은 `null + reason` 원칙을 사용한다.
- 질의응답과 위험 분석 설명은 가능한 경우 `clause evidence`를 포함한다.
- 위험 분석은 룰 기반 탐지를 우선하고, LLM은 설명 생성과 보정에 사용한다.
- 비전 탐지 결과는 텍스트 분석 결과와 별도로 보존한 뒤 최종 응답에서 함께 결합한다.
- 시연 안정성을 정확도 개선보다 우선한다.

### 단계별 세부 작업

#### 1. 입력 및 OCR

- 텍스트 입력은 원문을 그대로 사용한다.
- PDF, 이미지 입력은 PaddleOCR을 통해 텍스트를 추출한다.
- OCR 결과는 줄 단위 원본과 정제 텍스트를 함께 유지한다.
- 품질이 낮은 경우 재시도 가능한 전처리 지점을 분리한다.

#### 2. 전처리 및 조항 분리

- 공백, 줄바꿈, 머리말/꼬리말 노이즈 제거 규칙을 정의한다.
- `제1조`, `제2조`, 숫자 목록, 제목 패턴을 기준으로 조항을 분리한다.
- 조항 분리 실패 시 전체 문서를 단일 조항 리스트로라도 유지한다.

#### 3. 필드 추출과 분류

- 정규표현식으로 날짜, 금액, 계약 기간, 당사자명 등 후보값을 먼저 추출한다.
- LLM은 계약 유형 분류와 후보값 보정에 사용한다.
- 추출 실패 항목은 `null`로 채우고 사유를 남긴다.

#### 4. 위험 분석, 요약, 질의응답

- 자동 갱신, 일방 해지, 손해배상, 모호한 지급 조건 등 핵심 리스크 룰을 우선 구현한다.
- 요약은 계약 목적, 기간, 금액, 주요 의무, 주요 리스크를 중심으로 제한된 형식으로 생성한다.
- 질의응답은 조항 검색 후 근거 조항을 포함한 답변을 생성한다.

#### 5. 비전 탐지

- 탐지 대상은 인감, 서명, 체크 표식으로 제한한다.
- 탐지 결과는 bbox, label, confidence 단위로 저장 가능해야 한다.
- 탐지 결과는 텍스트 분석과 독립적으로 실패 가능하며, 실패 시 전체 분석 흐름을 중단하지 않는다.

## 5. 최소 인터페이스 정의

이 단계에서는 코드 API를 확정하지 않지만, 구현 착수에 필요한 최소 I/O 형식은 아래 기준으로 고정한다.

### OCR 출력 형태

```python
{
    "document_id": "str",
    "source_type": "text|pdf|image",
    "raw_text": "str",
    "normalized_text": "str",
    "pages": [
        {
            "page_index": 0,
            "text": "str"
        }
    ]
}
```

### Clause intermediate 형태

```python
{
    "clause_id": "str",
    "title": "str | None",
    "text": "str",
    "page_refs": [0],
    "order": 1
}
```

### Extraction 결과 형태

```python
{
    "contract_type": {
        "value": "nda|service|employment|unknown",
        "reason": "str | None"
    },
    "counterparty_a": {
        "value": "str | None",
        "reason": "str | None"
    },
    "counterparty_b": {
        "value": "str | None",
        "reason": "str | None"
    },
    "signing_date": {
        "value": "YYYY-MM-DD | None",
        "reason": "str | None"
    },
    "start_date": {
        "value": "YYYY-MM-DD | None",
        "reason": "str | None"
    },
    "end_date": {
        "value": "YYYY-MM-DD | None",
        "reason": "str | None"
    },
    "amount_text": {
        "value": "str | None",
        "reason": "str | None"
    },
    "amount_value": {
        "value": "float | None",
        "reason": "str | None"
    }
}
```

### Risk 결과 형태

```python
{
    "risk_type": "auto_renewal|termination|liability|payment|other",
    "severity": "low|medium|high",
    "reason": "str",
    "evidence_clause_ids": ["str"],
    "evidence_text": "str"
}
```

### Summary / QA 결과 형태

```python
{
    "summary": "str",
    "qa": {
        "question": "str",
        "answer": "str",
        "evidence_clause_ids": ["str"]
    }
}
```

### Vision detection 형태

```python
{
    "page_index": 0,
    "label": "seal|signature|checkmark",
    "confidence": 0.0,
    "bbox": {
        "x1": 0,
        "y1": 0,
        "x2": 0,
        "y2": 0
    }
}
```

## 6. 모듈별 작업 항목

### `src/chains`

- 계약 분석 체인의 입력, 중간 산출물, 최종 결과 구조를 정의한다.
- OCR 결과와 조항 분리 결과를 받아 분류, 추출, 요약, 질의응답을 호출하는 오케스트레이션 골격을 만든다.
- LangChain 연결 지점과 모델 호출 실패 처리 규칙을 정리한다.

### `src/ocr`

- PaddleOCR 기반 추출 파이프라인을 표준화한다.
- 페이지 단위 결과와 정제 텍스트를 함께 반환하는 형태를 만든다.
- OCR 실패, 품질 저하, 빈 결과에 대한 예외 케이스를 정의한다.

### `src/vision`

- YOLOv8 추론 입력/출력 인터페이스를 표준화한다.
- 인감, 서명, 체크 표식 클래스 기준을 고정한다.
- 텍스트 분석 실패와 독립적인 비전 처리 흐름을 만든다.

### `src/prompts`

- 계약 유형 분류 프롬프트
- 핵심 정보 추출 프롬프트
- 위험 설명 보조 프롬프트
- 요약 프롬프트
- 근거 기반 질의응답 프롬프트

각 프롬프트는 출력 형식, 근거 포함 여부, 실패 시 응답 정책을 포함해야 한다.

### `src/evaluation`

- OCR 정확도 평가 기준
- 핵심 정보 추출 정확도 평가 기준
- 위험 조항 탐지 회귀 테스트 기준
- 비전 탐지 정확도 및 시연 성공 기준
- 요약/Q&A 품질 점검 항목

평가 결과는 최소한 샘플 계약서 3종 기준으로 반복 실행 가능해야 한다.

### `training`

- Roboflow 데이터셋 버전 정보 정리
- YOLOv8 학습 설정 관리
- Lightning 실험 재현 정보 정리
- 추론용 가중치와 학습용 설정 파일 분리

## 7. 주차별 실행 계획

### 1주차: 환경 연결과 골격 고정

#### 목표

- 저장소 기준 AI 파이프라인의 뼈대를 먼저 고정한다.

#### 작업

- Gemini API 연결 테스트
- LangChain 기본 체인 연결 테스트
- PaddleOCR 실행 테스트
- YOLOv8/Roboflow 자산 확인
- `src/chains` 체인 골격 정의
- 구조화 출력 스키마 초안 작성
- `src/evaluation` 평가 기준 초안 작성

#### 산출물

- 체인 입력/출력 명세 초안
- 계약 유형 분류 프롬프트 초안
- 평가 항목 목록
- 샘플 계약서 3종 목록

### 2주차: 파서와 추출 파이프라인 연결

#### 목표

- 조항 분리와 핵심 정보 추출이 실제 입력에서 동작해야 한다.

#### 작업

- OCR 정제 로직 구현
- 조항 분리 로직 구현
- 필드 후보 추출 구현
- 계약 유형 분류 연결
- 핵심 필드 추출 연결
- `null + reason` 규칙 적용

#### 산출물

- 조항 단위 intermediate 구조
- 핵심 필드 추출 결과 샘플
- 파서 테스트 케이스
- OCR 실패/예외 처리 규칙

### 3주차: 리스크, 요약, 질의응답

#### 목표

- 사용자가 볼 핵심 분석 기능을 완성한다.

#### 작업

- 위험 조항 룰 구현
- LLM 기반 리스크 설명 보조 연결
- 요약 기능 연결
- QA 검색 흐름 정리
- 근거 조항 포함 답변 형식 고정

#### 산출물

- 리스크 룰셋 초안
- 요약 출력 형식
- QA 응답 예시 세트
- 회귀 테스트용 질문 목록

### 4주차: 탐지 통합과 평가 안정화

#### 목표

- 텍스트 분석과 비전 탐지를 결합하고 시연 안정성을 높인다.

#### 작업

- YOLOv8 탐지 결과 검증
- 탐지 결과와 텍스트 분석 결과 결합
- 샘플 계약서 3종 기준 통합 회귀 테스트
- OCR, 추출, 탐지, 요약, QA 오류 사례 정리
- 프롬프트 튜닝 및 출력 형식 안정화

#### 산출물

- 통합 분석 결과 예시
- 평가 결과 기록
- 실패 사례 목록
- 데모용 대표 입력 문서 세트

## 8. 외부 연동 가정

### 백엔드/API 팀과의 연동

- 입력: 문서 ID, 원본 파일 경로 또는 파일 바이트, 문서 형식 정보
- 출력: OCR 결과, 조항 리스트, 추출 결과, 리스크 리스트, 요약, QA 결과, 비전 탐지 결과
- 상태값: `pending`, `running`, `completed`, `failed` 수준의 작업 상태를 가정한다.

### 저장 계층 가정

- 본 저장소는 저장 구현 자체보다 결과 구조 안정화를 우선한다.
- 백엔드는 AI 결과를 DB에 저장 가능하도록 JSON 직렬화 가능한 형태를 요구한다고 가정한다.

### 데이터셋 가정

- 시연용 기준 데이터는 계약서 3종 이상을 확보한다.
- 비전 탐지 데이터셋은 Roboflow 버전 기준으로 관리한다.

## 9. 평가 및 완료 기준

### 평가 항목

- OCR: 문서 단위로 읽을 수 있는 텍스트가 안정적으로 확보되는지 확인
- 추출: 계약 유형, 당사자, 날짜, 금액, 기간 추출이 재현 가능한지 확인
- 리스크: 대표 위험 조항을 최소 1개 이상 탐지 가능한지 확인
- 요약: 계약 목적과 주요 리스크를 빠짐없이 포함하는지 확인
- 질의응답: 질문 3개 이상에 대해 근거 조항 포함 답변이 가능한지 확인
- 탐지: 인감 또는 서명 또는 체크 표식이 화면에 표시 가능한 수준으로 검출되는지 확인

### 완료 기준

- 샘플 계약서 3종이 끝까지 분석된다.
- 핵심 정보 추출 결과가 구조화 형태로 반환된다.
- 위험 조항과 근거 조항이 함께 반환된다.
- 비전 탐지 결과가 별도 구조로 반환된다.
- QA 응답이 근거 조항 ID를 포함한다.
- 주요 실패 케이스에 대한 예외 메시지 또는 실패 사유가 남는다.

### 검증 체크리스트

- 저장소의 실제 디렉터리와 계획 항목이 1:1로 대응되는지 확인한다.
- `../team-docs`의 핵심 요구사항이 누락 없이 반영되었는지 확인한다.
- 팀 전체 업무와 AI 저장소 업무가 섞이지 않았는지 확인한다.
- 4주 일정 안에서 각 주차 산출물이 실행 가능한 수준인지 확인한다.
- 평가 항목에 OCR, 추출, 탐지, 요약, Q&A 안정성이 모두 포함되었는지 확인한다.

## 10. 기본 가정

- 문서는 루트 `README.md` 옆 단일 파일로 관리한다.
- 문서명은 `AI_DEVELOPMENT_PLAN.md`를 사용한다.
- 상세 API 설계와 DB 구현은 별도 저장소 또는 백엔드 담당 영역에서 확정한다.
- 구현 단계에 들어가면 `src/evaluation/README.md`는 세부 평가 기준 문서, 본 문서는 상위 실행 계획 문서로 분리 운영한다.
